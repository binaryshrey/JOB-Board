# Hand-curated Ashby board slugs -- one slug or full URL per line, `#` comments.
#
# This is the manual escape hatch for companies no automated source reaches:
# something you heard about in a Discord, a referral, a job post that was never
# crawled. Add freely -- `validate` discards anything that does not resolve.
#
# Deliberately empty: the original contents were seeded from Common Crawl
# output, so every entry was already found automatically and the file scored
# zero unique boards. Only add slugs here that discovery genuinely misses.
#
# example:
#   acme-robotics
#   https://jobs.ashbyhq.com/acme-robotics
